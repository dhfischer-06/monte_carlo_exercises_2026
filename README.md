To run code,
First time:
 - ensure uv by astral is installed
 - run the command "uv sync" to automatically create the .venv
After that:
 - run with "uv run {filename}"

 To manage packages, use the command "uv add {package name}".
 Then, commit and push the changes so that upon pulling changes
 we will be up-to-date on packages.